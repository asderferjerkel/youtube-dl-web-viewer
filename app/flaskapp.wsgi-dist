import sys

# Set the path to the folder containing app/ (e.g. /path/to/youtube-dl-web-viewer)
sys.path.insert(0, '/var/www/html/youtube-dl-web-viewer')

from app import create_app
application = create_app()